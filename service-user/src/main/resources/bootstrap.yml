spring:
  application:
    name: service-user
  cloud:
    config:
      label: master
      profile: default
      discovery:
        enabled: true
        service-id: config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/

server:
  port: 3000
