spring:
  application:
    name: api-gateway
server:
  port: 1200

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/
zuul:
  routes:
    service-user:
      path: /user/**
      servie-id: service-user