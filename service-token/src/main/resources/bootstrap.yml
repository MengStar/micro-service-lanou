spring:
  application:
    name: service-token
  cloud:
    config:
      label: master
      profile: default
      discovery:
        enabled: true
        service-id: config-server
      fail-fast: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/

server:
  port: 2100